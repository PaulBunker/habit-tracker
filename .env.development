# Development/Sandbox Environment
NODE_ENV=development

# Database path - isolated sandbox data (relative to monorepo root)
DB_PATH=./packages/backend/data/dev

# Backend server port
PORT=3001

# Frontend dev server port (used by Vite)
VITE_PORT=5174

# Backend URL for frontend proxy
VITE_API_URL=http://localhost:3001
