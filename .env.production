# Production Environment
NODE_ENV=production

# Database path - user's home directory for persistent data
DB_PATH=~/.habit-tracker/data

# Backend server port
PORT=3000

# Frontend dev server port (used by Vite)
VITE_PORT=5173

# Backend URL for frontend proxy
VITE_API_URL=http://localhost:3000
